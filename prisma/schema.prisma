datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Product {
  id            String @id @default(cuid())
  title         String @db.VarChar(255)
  description   String
  image         String
  price         Int
}

